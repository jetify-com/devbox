{
  "packages": [
    "elixir@latest"
  ],
  "shell": {
    "init_hook": [
      "mix deps.get"
    ],
    "scripts": {
      // run_test is temporarily disabled. This example installs Hex from
      // builds.hex.pm (via `mix deps.get`), which currently fails in CI with a
      // TLS certificate validation error. This is a flaky/environmental issue
      // unrelated to dependency versions. The examples test runner skips any
      // example without a run_test script. Re-enable in a follow-up once the
      // Hex/TLS flake is resolved.
      // "run_test": "mix run"
    }
  }
}
