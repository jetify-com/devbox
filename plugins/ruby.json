{
  "name": "ruby",
  "version": "0.0.2",
  "env": {
    "PATH": "{{ .Virtenv }}/bin/:$PATH",
    "RUBY_CONFDIR": "{{ .Virtenv }}",
    "GEMRC": "{{ .Virtenv }}/.gemrc",
    "GEM_HOME": "{{ .Virtenv }}"
  }
}
