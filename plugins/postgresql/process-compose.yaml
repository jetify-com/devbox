version: "0.5"

processes:
  postgresql:
    command: "pg_ctl start -l $PGHOST/logfile -o \"-k $PGHOST\""
    isDaemon: true
    shutdown: 
      command: "pg_ctl stop -m fast"
    availability:
      restart: "on_failure"
      max_restarts: 5