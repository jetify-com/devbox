events {}
http { 
    # This doesn't work, what needs to be installed here?
    # include mime.types;
    sendfile on;
    

    server {

        access_log /tmp/access.log;

        listen 80;
        listen [::]:80;

        resolver 127.0.0.11;
        autoindex off;

        server_name _;
        server_tokens off;

        # root /Users/mike/dev/devbox/testdata/nginx/static/;
        root ./static/;
        
        gzip_static on;
    }
}
