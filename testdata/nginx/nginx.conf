# Can this be done with config?
user root root;

events {}
http { 
    # This doesn't work, what needs to be installed here?
    # include mime.types;
    sendfile on;
    

    server {

        listen 80;
        listen [::]:80;

        resolver 127.0.0.11;
        autoindex off;

        server_name _;
        server_tokens off;

        root /app/static;
        gzip_static on;
    }
}
