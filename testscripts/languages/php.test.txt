exec devbox run php index.php
stdout 'ds-loaded\n'

exec devbox rm php85Extensions.ds
exec devbox run php index.php
stdout 'ds-not-loaded\n'

exec devbox add php85Extensions.ds
exec devbox run php index.php
stdout 'ds-loaded\n'

-- devbox.json --
{
  "packages": [
    "php@8.5",
    "php85Extensions.ds@latest"
  ]
}

-- index.php --
<?php

echo extension_loaded('ds') ? "ds-loaded\n" : "ds-not-loaded\n";
